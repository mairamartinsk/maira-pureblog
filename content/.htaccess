Options -Indexes

# Block direct access to .md files
<FilesMatch "\.md$">
    Require all denied
</FilesMatch>

# Block directories/files that are only used via PHP server-side includes
RewriteEngine On
RewriteRule ^layouts/ - [F,L]
RewriteRule ^functions\.php$ - [F,L]
